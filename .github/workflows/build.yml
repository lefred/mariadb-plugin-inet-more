name: Build and package INET_MORE plugin

on:
  push:
    branches:
      - main
    tags:
      - "v*"

  pull_request:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    uses: lefred/mariadb-plugin-ci/.github/workflows/build-plugin.yml@v1
    with:
      plugin_name: inet-more
      plugin_directory: inet_more
      build_target: inet_more
      plugin_library: inet_more.so
      cmake_options: -DPLUGIN_DATE_EXTRA=DYNAMIC
      test_suite: inet_more
      run_tests: ${{ startsWith(github.ref, 'refs/tags/v') }}
